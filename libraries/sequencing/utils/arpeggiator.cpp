#include "arpeggiator.h"

Arpeggiator::Arpeggiator(Chord chord, arp_type type, int octaves) 
                        : chord(chord), type(type), octaves(octaves)
{
    
}

pair<int, double> Arpeggiator::note_for_step(int step) 
{
    
}
